# 片上外设详细讲解
> **适合对象：** 刚开始学习单片机、嵌入式、STM32、51、ESP32、ARM Cortex-M 的同学

## 目录
1. 什么是片上外设
2. CPU、寄存器、总线和外设的关系
3. 学习片上外设前必须理解的几个概念
4. GPIO：最基础的外设
5. 中断：让 CPU 不用一直等待
6. 定时器 Timer：嵌入式里的“秒表”和“节拍器”
7. PWM：用数字信号控制模拟效果
8. UART/USART：最常用的串口通信
9. I2C：两根线连接多个芯片
10. SPI：速度快、结构简单的同步通信
11. ADC：把模拟电压变成数字
12. DAC：把数字变成模拟电压
13. DMA：让数据搬运不再占用 CPU
14. 看门狗 Watchdog：防止程序跑飞
15. RTC：低功耗实时时钟
16. Flash、SRAM、EEPROM 与存储相关外设
17. 时钟系统 RCC/Clock：所有外设运行的基础
18. 电源与低功耗外设
19. CAN、USB、以太网等高级通信外设
20. 外设初始化的一般套路
21. 新手常见问题和排查方法
22. 推荐学习路线

## 1. 什么是片上外设

“片上外设”中的“片”指芯片，也就是单片机、微控制器 MCU、SoC 这类芯片。
“外设”不是指外面接的模块，而是指芯片内部集成的功能模块。

**简单理解：**
CPU 负责运行程序，片上外设负责和外部世界打交道，或者帮 CPU 完成某些专门工作。

**常见片上外设包括：**
- **GPIO：** 控制引脚高低电平，读取按键状态。
- **UART：** 串口通信，用来连接电脑、蓝牙模块、GPS 模块等。
- **I2C：** 连接传感器、EEPROM、OLED 屏幕。
- **SPI：** 连接屏幕、Flash、无线模块、ADC 芯片。
- **ADC：** 读取模拟电压，比如电位器、电池电压、温度传感器。
- **定时器：** 定时、计数、产生 PWM、测量脉冲。
- **DMA：** 自动搬运数据，减轻 CPU 负担。
- **RTC：** 记录真实时间，比如年月日时分秒。
- **看门狗：** 程序异常卡死时自动复位系统。
- **USB、CAN、以太网：** 更复杂的通信功能。

一个单片机之所以好用，通常不是因为 CPU 有多强，而是因为它集成了很多外设。
**比如你想做一个温湿度采集器，CPU 只负责逻辑，实际工作可能是：**
- GPIO 读取按键。
- I2C 读取温湿度传感器。
- UART 打印调试信息。
- ADC 采集电池电压。
- 定时器每 1 秒触发一次采集。
- RTC 记录采集时间。
- Flash 保存历史数据。

## 2. CPU、寄存器、总线和外设的关系

**很多初学者会疑惑：** 程序到底是怎么控制外设的？

**答案是：** CPU 通过读写寄存器来控制外设。

寄存器可以理解成外设内部的一些“控制开关”和“状态窗口”。
**比如 GPIO 外设中可能有这些寄存器：**
- **方向寄存器：** 决定引脚是输入还是输出。
- **输出寄存器：** 决定输出高电平还是低电平。
- **输入寄存器：** 读取外部引脚当前是高还是低。
- **复用功能寄存器：** 决定这个引脚是普通 GPIO，还是 UART、SPI、I2C 等功能。

程序里看到的外设配置，本质上都是在写寄存器。

**例如伪代码：**
- 设置 PA5 为输出模式。
- 让 PA5 输出高电平。
- 等待一段时间。
- 让 PA5 输出低电平。

**底层对应的其实是：**
- 写 GPIOA 的模式寄存器。
- 写 GPIOA 的输出数据寄存器。

CPU 和外设之间通过总线连接。
常见总线包括 AHB、APB 等。
**你不用一开始就深入总线协议，但要知道一个结论：**
不同外设挂在不同总线上，它们通常需要先打开对应的时钟，才能工作。

**这就是为什么很多单片机代码第一步会写：**
- 打开 GPIOA 时钟。
- 打开 USART1 时钟。
- 打开 TIM2 时钟。

没有时钟，外设就像没有通电一样，寄存器写了也可能没有效果。

## 3. 学习片上外设前必须理解的几个概念

### 3.1 引脚 Pin

引脚是芯片和外部电路连接的金属脚。
一个引脚可能有多个功能。
比如 PA9 可以当普通 GPIO，也可以当 USART1_TX，还可能当定时器输出。

这叫“引脚复用”。
同一个引脚在同一时刻通常只能选择一种功能。

### 3.2 电平

**数字电路里常见两个状态：**
- **高电平：** 通常接近 3.3V 或 5V。
- **低电平：** 通常接近 0V。

MCU 的 IO 电压要和外部模块匹配。
如果 MCU 是 3.3V，而外部模块输出 5V，就可能烧坏 IO，除非芯片说明书明确写着该引脚 5V 容忍。

### 3.3 寄存器

寄存器是外设的配置入口。
使用库函数时你可能看不到寄存器细节，但库函数最终还是会操作寄存器。

**常见写法有三种：**
- **直接寄存器开发：** 最贴近硬件，难度高。
- **标准外设库或 HAL 库：** 更容易上手，适合大多数项目。
- **LL 库或轻量驱动：** 比 HAL 更底层，效率高一些。

### 3.4 时钟

外设需要时钟才能运行。
CPU 有 CPU 时钟，外设有外设时钟，定时器、串口、ADC 也有自己的时钟来源或分频。

**很多外设问题最后都和时钟有关：**
- 串口波特率不对。
- 定时器周期不准。
- ADC 采样时间异常。
- I2C 速度不对。
- USB 无法枚举。

### 3.5 中断

中断是外设通知 CPU 的一种机制。
例如串口收到一个字节，可以触发中断，让 CPU 立刻去处理。

**没有中断时，CPU 可能要一直问：**
- 串口收到数据了吗？
- 收到数据了吗？
- 收到数据了吗？

这叫轮询。

有中断时，CPU 可以先做别的事，等外设有事再通知 CPU。

### 3.6 轮询、中断、DMA 的区别

**轮询：**
- CPU 一直检查外设状态。
- **优点：** 简单。
- **缺点：** 浪费 CPU。

**中断：**
- 外设有事件时打断 CPU。
- **优点：** 效率高，响应快。
- **缺点：** 程序结构稍复杂。

**DMA：**
- 外设和内存之间自动搬运数据。
- **优点：** 适合大量数据，CPU 负担小。
- **缺点：** 配置更复杂，调试难度更高。

## 4. GPIO：最基础的外设

GPIO 全称 General Purpose Input Output，通用输入输出。
这是最基础、最常用的片上外设。

**GPIO 可以做什么：**
- 点亮 LED。
- 读取按键。
- 控制继电器。
- 读取数字传感器输出。
- 控制片选信号 CS。
- 模拟简单通信时序。

### 4.1 GPIO 输出

输出模式下，MCU 控制引脚输出高电平或低电平。

**例子：** 点亮 LED。
- 如果 LED 正极接 3.3V，负极通过电阻接 MCU 引脚，那么 MCU 输出低电平时 LED 亮。
- 如果 LED 正极通过电阻接 MCU 引脚，负极接 GND，那么 MCU 输出高电平时 LED 亮。

所以不要死记“高电平 LED 亮”，要看电路连接方式。

### 4.2 GPIO 输入

输入模式下，MCU 读取引脚上的电平。

**例子：** 读取按键。
- 按键一端接 GND，另一端接 MCU 引脚。
**如果开启上拉电阻：**
- 没按下时，引脚被拉到高电平。
- 按下时，引脚接到 GND，读到低电平。

**因此很多按键逻辑是：**
- 读到 0 表示按下。
- 读到 1 表示松开。

### 4.3 上拉和下拉

输入引脚不能悬空。
悬空时，电平不确定，可能一会儿是 0，一会儿是 1。

- **上拉：** 通过电阻把引脚拉到高电平。
- **下拉：** 通过电阻把引脚拉到低电平。

很多 MCU 内部自带上拉和下拉，可以通过软件开启。

### 4.4 推挽输出和开漏输出

**推挽输出：**
可以主动输出高电平，也可以主动输出低电平。
适合 LED、普通控制信号。

**开漏输出：**
只能主动拉低，不能主动输出高电平。
高电平需要外部或内部上拉电阻提供。
常用于 I2C 总线、多设备共享信号线。

### 4.5 GPIO 常见坑

- 忘记打开 GPIO 时钟。
- 引脚模式没有配置对。
- LED 电路是低电平点亮，程序却一直输出高电平。
- 按键没有上拉或下拉，导致输入乱跳。
- 引脚被复用成其他功能，不能当普通 GPIO 用。
- 外部模块电压不匹配，可能损坏芯片。

## 5. 中断：让 CPU 不用一直等待

中断可以理解为“外设有急事时打电话给 CPU”。

**比如：**
- 按键按下了，触发外部中断。
- 串口收到数据了，触发串口接收中断。
- 定时器时间到了，触发定时器更新中断。
- ADC 转换完成了，触发 ADC 完成中断。

### 5.1 为什么需要中断

假设你要一边闪 LED，一边接收串口数据。
如果不用中断，程序可能一直卡在等待串口数据的地方，LED 就不能稳定闪烁。

**使用中断后：**
- 主循环负责闪 LED。
- 串口收到数据时，中断函数临时处理接收。
- 处理完后回到主循环。

### 5.2 中断服务函数

中断触发后，CPU 会暂停当前代码，跳到中断服务函数执行。
中断服务函数一般叫 ISR，Interrupt Service Routine。

中断函数里应该尽量短。
不要在中断里做耗时操作，比如长延时、大量打印、复杂计算。

**推荐做法：**
- 中断里只读取数据、清标志位、设置标志变量。
- 主循环里根据标志变量做复杂处理。

### 5.3 中断优先级

多个中断同时发生时，需要决定谁先执行。
这就是中断优先级。

优先级高的中断可以先执行，甚至可以打断优先级低的中断。

**初学阶段先记住：**
- 不是所有中断都应该设成最高优先级。
- 越紧急、越不能丢的事件，优先级越高。
- 耗时处理不要放在高优先级中断里。

### 5.4 外部中断 EXTI

外部中断常用于按键、传感器报警脚、模块唤醒脚。

**触发方式有：**
- **上升沿触发：** 从低电平变成高电平。
- **下降沿触发：** 从高电平变成低电平。
- **双边沿触发：** 上升和下降都触发。

按键一般还要考虑消抖。
机械按键按下瞬间会抖动，可能触发多次中断。
可以通过软件延时、定时器消抖、状态机消抖来解决。

## 6. 定时器 Timer：嵌入式里的“秒表”和“节拍器”

定时器是非常重要的外设。
它本质上是一个会自动计数的硬件计数器。

**定时器可以做：**
- 固定时间间隔触发中断。
- 产生 PWM。
- 测量输入脉冲宽度。
- 计算外部信号频率。
- 驱动电机控制。
- 实现超时判断。

### 6.1 定时器的核心参数

- **定时器时钟频率：** 定时器每秒收到多少个计数脉冲。
- **预分频器 PSC：** 把输入时钟变慢。
- **自动重装值 ARR：** 计数到多少后溢出。
- **计数器 CNT：** 当前计数值。

**常见计算公式：**
定时器更新频率 = 定时器时钟 / (PSC + 1) / (ARR + 1)
定时器周期 = 1 / 定时器更新频率

**例子：**
- 定时器时钟 72 MHz。
- PSC = 7199，则计数频率 = 72 MHz / 7200 = 10 kHz。
- ARR = 9999，则更新频率 = 10 kHz / 10000 = 1 Hz。
- 也就是每 1 秒触发一次更新中断。

### 6.2 基本定时

如果想每 10 ms 做一次任务，可以用定时器中断。
定时器到时间后进入中断，在中断里设置一个标志。
主循环看到标志后执行任务。

### 6.3 输入捕获

输入捕获用于测量外部信号。
例如超声波测距模块会输出一个高电平脉冲，脉宽代表距离。
定时器可以记录上升沿和下降沿发生时的计数值。
两者相减就得到脉冲宽度。

### 6.4 输出比较

输出比较可以在计数器达到指定值时改变引脚状态。
PWM 就是输出比较的一种常见应用。

### 6.5 定时器常见坑

- 定时器时钟频率没有算对。
- PSC 和 ARR 都要加 1 参与计算。
- 忘记开启定时器中断。
- 忘记清除中断标志位。
- 中断函数名字写错。
- 定时器引脚复用配置错误。

## 7. PWM：用数字信号控制模拟效果

PWM 全称 Pulse Width Modulation，脉宽调制。
它是用快速变化的高低电平模拟出不同“平均效果”的方法。

**PWM 有两个核心参数：**
- **频率：** 一秒重复多少次。
- **占空比：** 一个周期内高电平占多大比例。

- **占空比 0%：** 一直低电平。
- **占空比 50%：** 一半时间高，一半时间低。
- **占空比 100%：** 一直高电平。

**PWM 可以做：**
- 调 LED 亮度。
- 控制直流电机速度。
- 控制舵机角度。
- 产生简单音调。
- 控制电源转换电路。

### 7.1 LED 调光

人眼有视觉暂留。
如果 LED 快速闪烁，人眼会觉得它是持续亮的。
占空比越高，平均亮度越高。

### 7.2 电机调速

PWM 控制电机驱动芯片，让电机获得不同的平均电压。
占空比越高，电机转速通常越快。

**注意：** MCU 引脚不能直接驱动大电机。
必须通过驱动芯片、MOS 管或电机驱动模块。

### 7.3 舵机控制

常见舵机使用 50 Hz PWM，也就是周期约 20 ms。
脉宽大约 0.5 ms 到 2.5 ms 对应不同角度。

不同舵机的具体范围可能不同，需要看说明书。

### 7.4 PWM 常见坑

- 频率设置不合适，LED 可能闪烁。
- 占空比计算错误。
- 定时器通道和引脚没有对应上。
- 没有配置引脚复用输出。
- 电机没有独立供电或没有共地。

## 8. UART/USART：最常用的串口通信

UART 是异步串口通信。
USART 比 UART 多了同步能力，但实际项目中常把 USART 当 UART 用。

**串口常用于：**
- 连接电脑打印调试日志。
- 连接蓝牙、Wi-Fi、GPS、4G 模块。
- MCU 之间简单通信。

### 8.1 串口线怎么接

**最少三根线：**
- **TX：** 发送。
- **RX：** 接收。
- **GND：** 地。

**连接规则：**
- 设备 A 的 TX 接设备 B 的 RX。
- 设备 A 的 RX 接设备 B 的 TX。
- GND 必须共地。

很多新手串口不通，就是 TX/RX 没有交叉，或者忘记共地。

### 8.2 波特率

波特率表示每秒传输多少位。
**常见值：** 9600、115200、921600。

通信双方波特率必须一致。
如果一边 9600，一边 115200，收到的数据就是乱码。

### 8.3 数据格式

**常见配置是 8N1：**
- 8 个数据位。
- No parity，无校验。
- 1 个停止位。

双方的数据位、校验位、停止位也必须一致。

### 8.4 串口发送

发送可以用阻塞方式、中断方式、DMA 方式。

**阻塞发送：**
简单，但发送期间 CPU 可能一直等待。

**中断发送：**
发送过程由中断推进，CPU 可以做别的事。

**DMA 发送：**
适合大量连续数据，比如日志、图像数据。

### 8.5 串口接收

串口接收比发送更容易出问题，因为你不知道数据什么时候来、来多少。

**常见接收方式：**
- **轮询接收：** 简单但容易漏数据。
- **中断逐字节接收：** 常用，适合命令协议。
- **DMA + 空闲中断：** 适合不定长数据包，高效。

### 8.6 串口常见坑

- TX/RX 没有交叉。
- 没有共地。
- 波特率不一致。
- 电平不匹配，TTL 串口和 RS232 不能直接接。
- 没有打开串口时钟或 GPIO 时钟。
- 引脚复用配置错误。
- 接收缓冲区太小导致数据丢失。
- 中断里处理太久导致后续数据丢失。

## 9. I2C：两根线连接多个芯片

I2C 是一种同步串行通信总线。
**只需要两根信号线：**
- **SCL：** 时钟线。
- **SDA：** 数据线。

**I2C 常用于连接：**
- 温湿度传感器。
- 气压传感器。
- OLED 屏幕。
- EEPROM。
- RTC 芯片。
- 触摸芯片。

### 9.1 I2C 的特点

一条总线上可以挂多个设备。
每个设备有自己的地址。
主机发起通信，从机响应。

大多数情况下 MCU 是主机，传感器是从机。

### 9.2 上拉电阻

I2C 的 SCL 和 SDA 通常是开漏结构，需要上拉电阻。
没有上拉电阻，总线可能无法正常输出高电平。

**常见上拉电阻取值：** 2.2k、4.7k、10k。
具体取值和总线速度、线长、设备数量有关。

### 9.3 I2C 地址

I2C 设备都有地址。
常见说法有 7 位地址和 8 位地址。

**很多新手容易搞混：**
- 有的手册写 7 位地址，比如 0x3C。
- 有的库函数要求传入左移一位后的地址，比如 0x78。

遇到 I2C 不通时，要先确认库函数需要的是哪种地址格式。

### 9.4 I2C 通信过程

**典型读传感器寄存器的过程：**
- 主机发送起始信号。
- 主机发送从机地址和写方向。
- 主机发送要读取的寄存器地址。
- 主机再次发送起始信号。
- 主机发送从机地址和读方向。
- 从机返回数据。
- 主机发送停止信号。

### 9.5 I2C 常见坑

- SCL/SDA 没有上拉。
- 地址写错，特别是 7 位地址和 8 位地址混淆。
- 设备供电不对。
- 没有共地。
- 总线速度过高。
- 线太长导致波形变差。
- 多个设备地址冲突。
- 某个设备异常拉低总线，导致总线死锁。

## 10. SPI：速度快、结构简单的同步通信

SPI 是同步串行通信。
**常见信号线：**
- **SCK：** 时钟。
- **MOSI：** 主机输出，从机输入。
- **MISO：** 主机输入，从机输出。
- **CS 或 NSS：** 片选。

**SPI 常用于：**
- TFT 彩屏。
- 外部 Flash。
- SD 卡。
- 无线模块。
- 高速 ADC/DAC。

### 10.1 SPI 的特点

速度通常比 I2C 快。
协议简单。
没有统一地址机制，一般用 CS 片选不同设备。
全双工通信，发送的同时也能接收。

### 10.2 SPI 模式

SPI 有 4 种模式，由 CPOL 和 CPHA 决定。
CPOL 决定时钟空闲时是高还是低。
CPHA 决定在第几个边沿采样数据。

主机和从机的 SPI 模式必须一致，否则数据会错位。

### 10.3 片选 CS

多个 SPI 设备可以共用 SCK、MOSI、MISO。
每个设备单独一根 CS。
和哪个设备通信，就把哪个设备的 CS 拉低。

**很多设备要求：**
- 通信开始前 CS 拉低。
- 通信结束后 CS 拉高。

### 10.4 SPI 常见坑

- MOSI/MISO 接反。
- CS 没有控制好。
- SPI 模式不匹配。
- 时钟速度太高。
- 发送和接收长度不一致。
- 没有按芯片手册要求发送命令和地址。
- MISO 被多个设备同时驱动，通常是 CS 管理错误。

## 11. ADC：把模拟电压变成数字

ADC 全称 Analog to Digital Converter，模数转换器。
它把连续变化的模拟电压转换成数字值。

**应用：**
- 读取电位器。
- 读取电池电压。
- 读取光敏电阻。
- 读取温度传感器。
- 采集声音、电流、电压等信号。

### 11.1 分辨率

ADC 分辨率表示转换结果有多少级。

- **8 位 ADC：** 0 到 255。
- **10 位 ADC：** 0 到 1023。
- **12 位 ADC：** 0 到 4095。
- **16 位 ADC：** 0 到 65535。

如果参考电压是 3.3V，12 位 ADC 读数为 2048，电压大约是：
3.3V * 2048 / 4095 = 1.65V。

### 11.2 参考电压

ADC 不是凭空知道电压是多少，它是把输入电压和参考电压比较。
参考电压越稳定，测量越可靠。

如果参考电压波动，ADC 结果也会跟着波动。

### 11.3 采样时间

ADC 采样不是瞬间完成的。
输入信号源阻抗比较大时，需要更长采样时间，否则采样电容来不及充电，结果会偏差。

### 11.4 多通道 ADC

一个 ADC 模块通常可以测多个通道。
但很多 MCU 内部只有一个转换核心，通过多路选择器轮流测不同通道。

这意味着多通道采样不一定是完全同时的。

### 11.5 ADC 常见坑

- 输入电压超过允许范围。
- 没有共地。
- 参考电压不稳定。
- 采样时间太短。
- 模拟输入引脚没有配置成模拟模式。
- 电源噪声大导致读数跳动。
- 没有做滤波或平均。
- 分压电阻太大，导致采样误差。

### 11.6 简单滤波方法

**平均滤波：**
连续采样多次，取平均值。

**中值滤波：**
采样多次，排序后取中间值，适合去掉偶发尖峰。

**滑动平均：**
维护一个固定长度窗口，每次加入新数据，去掉旧数据，计算平均。

## 12. DAC：把数字变成模拟电压

DAC 全称 Digital to Analog Converter，数模转换器。
它和 ADC 相反，把数字值转换为模拟电压。

**用途：**
- 输出可变电压。
- 产生简单波形。
- 音频输出。
- 给模拟电路提供控制电压。

不是所有 MCU 都有 DAC。
如果没有 DAC，也可以用 PWM + 低通滤波来模拟一个粗略的模拟电压。

**DAC 常见坑：**
- 输出驱动能力有限，不能直接带大负载。
- 参考电压影响输出精度。
- 输出速度有限，不适合太高频率波形。
- 需要考虑滤波和噪声。

## 13. DMA：让数据搬运不再占用 CPU

DMA 全称 Direct Memory Access，直接存储器访问。
它可以在不让 CPU 一个字节一个字节搬运的情况下，把数据从一个地方搬到另一个地方。

**常见搬运方向：**
- **外设到内存：** 串口接收、ADC 采集。
- **内存到外设：** 串口发送、SPI 刷屏、DAC 输出波形。
- **内存到内存：** 复制数据。

### 13.1 为什么 DMA 很重要

假设串口高速接收大量数据。
如果每来一个字节 CPU 都进一次中断，CPU 会很忙。
使用 DMA 后，串口收到的数据可以自动写入内存缓冲区。
CPU 只需要在半满、全满或空闲中断时处理一批数据。

### 13.2 DMA 常见模式

**普通模式：**
传输指定长度后停止。

**循环模式：**
传输到缓冲区末尾后自动回到开头。
常用于 ADC 连续采样、音频数据流。

### 13.3 DMA 常见事件

- **半传输完成：** 缓冲区前半部分已完成。
- **传输完成：** 整个缓冲区已完成。
- **传输错误：** 配置或总线访问出错。

### 13.4 DMA 常见坑

- DMA 通道或请求源选错。
- 数据宽度配置错误，比如外设 8 位、内存却配置成 16 位。
- 内存地址是否递增配置错误。
- 缓冲区生命周期不对，局部变量被释放后 DMA 还在用。
- CPU 缓存导致数据不一致，高性能 MCU 上尤其要注意。
- 忘记处理半传输或传输完成标志。

## 14. 看门狗 Watchdog：防止程序跑飞

看门狗是一种安全保护外设。
它像一个倒计时器。
程序必须定期“喂狗”，也就是重置倒计时。
如果程序卡死，没有及时喂狗，看门狗就会让系统复位。

### 14.1 为什么需要看门狗

嵌入式设备可能长期运行。
程序可能因为电磁干扰、内存错误、异常逻辑进入死循环。
看门狗可以让设备自动恢复。

### 14.2 独立看门狗和窗口看门狗

**独立看门狗：**
通常使用独立低速时钟。
即使主时钟异常，也可能继续工作。
适合系统级保护。

**窗口看门狗：**
要求在指定时间窗口内喂狗。
喂得太早或太晚都可能复位。
可以检测程序运行节奏是否异常。

### 14.3 看门狗常见坑

- 调试时开启看门狗，断点停住后系统不断复位。
- 喂狗位置太随意，即使任务卡死也能喂狗，失去保护意义。
- 超时时间设置太短，正常任务偶尔耗时就复位。

**建议：**
不要在定时器中断里无条件喂狗。
更好的做法是确认关键任务都正常运行后再喂狗。

## 15. RTC：低功耗实时时钟

RTC 全称 Real Time Clock，实时时钟。
它可以记录年月日时分秒。

**应用：**
- 数据记录时间戳。
- 定时唤醒设备。
- 电子钟。
- 低功耗设备睡眠后仍保持时间。

RTC 通常使用 32.768 kHz 晶振。
这个频率常用于钟表，因为它很容易分频得到 1 秒。

**RTC 常见坑：**
- 没有备用电池，掉电后时间丢失。
- 低速晶振没有起振。
- 时区和夏令时处理混乱。
- 闹钟唤醒配置错误。
- 备份域没有正确初始化。

## 16. Flash、SRAM、EEPROM 与存储相关外设

### 16.1 Flash

Flash 用来保存程序，也可以保存少量用户数据。
**特点：**
- 掉电不丢失。
- 写入前通常要擦除。
- 擦除按页或扇区进行。
- 擦写次数有限。

不要频繁把计数器直接写进 Flash，否则容易把 Flash 写坏。

### 16.2 SRAM

SRAM 是运行时内存。
**特点：**
- 速度快。
- 掉电数据丢失。
- 用于变量、栈、堆、缓冲区。

### 16.3 EEPROM

有些 MCU 内置 EEPROM，有些没有。
EEPROM 适合保存参数，擦写比 Flash 更灵活。
没有 EEPROM 的 MCU 可以用 Flash 模拟 EEPROM。

### 16.4 存储常见坑

- Flash 写入前没有擦除。
- 擦除单位比写入数据大，误删同一页其他数据。
- 写入地址没有对齐。
- 频繁写同一地址，超过寿命。
- 掉电写入中断，导致数据损坏。

**实际项目里常用办法：**
- 加校验值。
- 加版本号。
- 双备份存储。
- 磨损均衡。

## 17. 时钟系统 RCC/Clock：所有外设运行的基础

时钟系统是 MCU 的心跳。
CPU、总线、外设都依赖时钟。

**常见时钟源：**
- 内部高速 RC 振荡器。
- 外部高速晶振。
- 内部低速 RC。
- 外部低速 32.768 kHz 晶振。
- PLL 倍频时钟。

### 17.1 为什么要配置时钟

**不同项目对时钟要求不同：**
- 低功耗项目希望频率低一些。
- 高性能项目希望 CPU 跑快。
- USB、以太网等外设可能要求精确时钟。
- 定时器、串口、ADC 都依赖时钟计算参数。

### 17.2 PLL

PLL 可以把较低频率的时钟倍频成更高频率。
例如外部 8 MHz 晶振，通过 PLL 变成 72 MHz 或 168 MHz。

### 17.3 外设时钟使能

很多 MCU 为了省电，外设默认时钟是关闭的。
使用外设前必须打开时钟。

**常见初始化顺序：**
打开 GPIO 时钟。
打开外设时钟。
配置引脚复用。
配置外设参数。
使能外设。

### 17.4 时钟常见坑

- 系统时钟和自己以为的不一样。
- 外设总线有分频，定时器时钟不等于 CPU 时钟。
- 串口波特率异常。
- USB 时钟不精确导致不能识别。
- 外部晶振不起振。
- 低功耗唤醒后时钟没有恢复。

## 18. 电源与低功耗外设

很多嵌入式设备用电池供电，所以低功耗很重要。

**常见低功耗模式：**
- **Sleep：** CPU 停止，外设可能继续运行。
- **Stop：** 大部分时钟停止，功耗更低。
- **Standby：** 接近掉电，只保留少量唤醒功能和备份区域。

**常见唤醒源：**
- 外部引脚中断。
- RTC 闹钟。
- 看门狗。
- 低功耗定时器。
- 串口或其他通信事件。

**低功耗常见坑：**
- 没有关闭不用的外设时钟。
- GPIO 悬空导致漏电。
- 外部模块仍在耗电。
- 调试器连接导致功耗测量不准。
- 唤醒后没有重新配置时钟或外设。

## 19. CAN、USB、以太网等高级通信外设

### 19.1 CAN

CAN 常用于汽车、工业控制。
**特点：**
- 抗干扰强。
- 多节点通信。
- 有仲裁机制。
- 需要 CAN 收发器芯片配合。

**常见坑：**
- 只有 MCU 的 CAN 控制器不够，还需要外部 CAN 收发器。
- 总线两端需要 120 欧终端电阻。
- 波特率和采样点要匹配。
- CANH、CANL 接线错误。

### 19.2 USB

USB 可以让 MCU 连接电脑，模拟串口、键盘、鼠标、U 盘等设备。

**常见类型：**
- **USB Device：** MCU 作为设备连接电脑。
- **USB Host：** MCU 作为主机连接 U 盘、键盘等。
- **USB OTG：** 可以在主机和设备间切换。

**常见坑：**
- USB 时钟要求严格。
- D+、D- 走线要注意。
- 枚举失败时要看描述符、时钟、电阻和线缆。
- 供电和地线必须可靠。

### 19.3 以太网

以太网用于网络通信。
MCU 内部可能只有 MAC，还需要外部 PHY 芯片。
实际开发还涉及 TCP/IP 协议栈，比如 lwIP。

**常见坑：**
- RMII/MII 引脚多，复用复杂。
- PHY 地址配置错误。
- 时钟源错误。
- 网络协议栈配置不当。

## 20. 外设初始化的一般套路

**大多数片上外设初始化都可以套用下面思路：**

1. **确认硬件连接**
   看原理图，确认引脚、供电、电平、上拉下拉、外部芯片型号。

2. **打开时钟**
   打开 GPIO 时钟和对应外设时钟。

3. **配置 GPIO**
   设置输入、输出、模拟、复用功能、上拉下拉、速度、开漏或推挽。

4. **配置外设参数**
   例如串口配置波特率，I2C 配置速度，定时器配置 PSC/ARR，ADC 配置通道。

5. **配置中断或 DMA**
   如果需要中断，配置 NVIC 和中断使能；如果需要 DMA，配置 DMA 通道、方向、宽度、地址递增、传输长度。

6. **启动外设**
   调用启动函数或设置使能位。

7. **验证结果**
   用 LED、串口打印、逻辑分析仪、示波器、万用表检查现象。

## 21. 新手常见问题和排查方法

### 21.1 程序没有现象

**检查：**
- 板子是否供电。
- 程序是否真的下载成功。
- 复位脚是否正常。
- 时钟是否启动。
- 对应 GPIO 是否配置正确。
- 外设时钟是否开启。

### 21.2 LED 不亮

**检查：**
- LED 是高电平亮还是低电平亮。
- 是否串了限流电阻。
- GPIO 是否设为输出。
- 引脚是否选错。
- 该引脚是否被其他功能占用。

### 21.3 按键读数乱跳

**检查：**
- 是否开启上拉或下拉。
- 按键电路是否正确。
- 是否需要消抖。
- 是否有长线干扰。

### 21.4 串口乱码

**检查：**
- 波特率是否一致。
- 数据位、停止位、校验位是否一致。
- 系统时钟是否配置正确。
- USB 转串口模块电平是否匹配。
- GND 是否共地。

### 21.5 I2C 找不到设备

**检查：**
- SCL/SDA 是否接反。
- 是否有上拉电阻。
- 地址是否正确。
- 设备是否供电。
- 是否共地。
- 总线是否被拉低。

### 21.6 SPI 读写错误

**检查：**
- SPI 模式是否正确。
- CS 是否正确拉低和拉高。
- MOSI/MISO 是否接反。
- 时钟频率是否过高。
- 命令格式是否符合芯片手册。

### 21.7 ADC 数值不稳定

**检查：**
- 电源是否干净。
- 参考电压是否稳定。
- 输入是否超范围。
- 采样时间是否足够。
- 是否需要滤波。
- 模拟地和数字地布局是否合理。

### 21.8 定时器时间不准

**检查：**
- 定时器时钟来源。
- 总线分频。
- PSC 和 ARR 计算是否加 1。
- 中断里是否有太多耗时操作。

## 22. 推荐学习路线

### 第一阶段：GPIO
**目标：**
- 点亮 LED。
- 实现 LED 闪烁。
- 读取按键。
- 按键控制 LED。

**你要理解：**
- 输入、输出、上拉、下拉、高低电平。

### 第二阶段：串口 UART
**目标：**
- 通过串口打印 Hello。
- 电脑发送命令，MCU 控制 LED。

**你要理解：**
- TX/RX 交叉、波特率、8N1、接收中断。

### 第三阶段：定时器和 PWM
**目标：**
- 定时器每 1 秒中断一次。
- PWM 调节 LED 亮度。
- PWM 控制舵机。

**你要理解：**
- PSC、ARR、占空比、频率计算。

### 第四阶段：I2C 和 SPI
**目标：**
- I2C 读取温湿度传感器。
- SPI 驱动屏幕或 Flash。

**你要理解：**
- 地址、片选、时钟、通信时序。

### 第五阶段：ADC 和 DMA
**目标：**
- ADC 读取电位器。
- ADC + DMA 连续采样。
- 串口 DMA 接收不定长数据。

**你要理解：**
- 采样时间、参考电压、缓冲区、半传输和全传输。

### 第六阶段：系统能力
**目标：**
- 加入看门狗。
- 使用 RTC 记录时间。
- 做低功耗唤醒。
- 把参数保存到 Flash。

**你要理解：**
- 系统可靠性、掉电保存、异常恢复、功耗控制。

## 总结

片上外设是 MCU 最核心的能力。
CPU 负责执行代码，外设负责连接外部世界和完成专门任务。
**学习外设时，不要只背库函数，要理解下面这条主线：**

- 硬件连接是否正确。
- 外设时钟是否打开。
- 引脚模式是否正确。
- 外设参数是否正确。
- 中断或 DMA 是否正确。
- 状态标志是否处理。
- 现象是否能用工具验证。

只要按这个思路学习，GPIO、串口、I2C、SPI、ADC、定时器这些外设都会变得清晰。
刚开始不需要一次性掌握所有细节，先从一个 LED、一个按键、一个串口开始，把“配置、运行、观察、排查”的流程练熟。
后面学习复杂外设时，本质还是同一套方法。
